name: Keep app awake
on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Card Desk
        run: curl -s -o /dev/null -m 30 https://card-desk.onrender.com/healthz || true
